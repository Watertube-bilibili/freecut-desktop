# FreeCut 桌面工作台

模式：Operate。用户明确要平替剪映，沿用专业非线性编辑器的工作习惯与中文版标签。已运行设计方向脚本；用户的竞品工作流方向优先于随机视觉候选。以视频审片室作为界面依据：中性深灰承载画面、薄分隔线明确区域、浅薄荷色仅用于主操作/选中/播放头。

工作区：顶部项目动作、左侧可搜索工具架、中间大预览、右侧属性检视器、底部多轨时间线。固定可见“关键帧”“自动字幕”“语音朗读”，不把高频功能埋入菜单。

颜色：背景 #101214，面板 #191c1f，抬升 #24282c，边界 #30353a，正文 #eef2f2，次要 #a9b3b8，强调 #91e6c6。轨道视频绿、叠加琥珀、音频蓝，用文字与图标共同区分。系统中文字体适配长时间工具使用，时间码使用等宽数字。按钮有 hover、focus、disabled、busy，所有图标来自 Lucide。紧凑控件而非卡片式页面。

首屏为“我的项目”首页：左侧项目/设置/关于，主区新建/打开、最近工程卡片和搜索排序。无项目时展示明确空状态，记录来自真实打开和保存，不扫描磁盘或伪造缩略图。关于页提供用户指定 B 站主页入口。

编辑器沿用原有布局；普通关键帧模式默认一键记录当前画面、常用滑块和整组导航，专业模式保留独立参数/缓动列表。工作台布局和关键帧模式是两个独立开关，偏好跨次启动保存。示例全部原创文字与色块，明确标为示例。退出或切换工程前保护未保存修改。
