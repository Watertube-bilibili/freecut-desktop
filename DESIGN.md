# FreeCut 桌面工作台

模式：Operate。用户明确要平替剪映，沿用专业非线性编辑器的工作习惯与中文版标签。已运行设计方向脚本；用户的竞品工作流方向优先于随机视觉候选。以视频审片室作为界面依据：中性深灰承载画面、薄分隔线明确区域、浅薄荷色仅用于主操作/选中/播放头。

工作区：顶部项目动作、左侧可搜索工具架、中间大预览、右侧属性检视器、底部多轨时间线。固定可见“关键帧”“自动字幕”“语音朗读”，不把高频功能埋入菜单。

颜色：背景 #101214，面板 #191c1f，抬升 #24282c，边界 #30353a，正文 #eef2f2，次要 #a9b3b8，强调 #91e6c6。轨道视频绿、叠加琥珀、音频蓝，用文字与图标共同区分。系统中文字体适配长时间工具使用，时间码使用等宽数字。按钮有 hover、focus、disabled、busy，所有图标来自 Lucide。紧凑控件而非卡片式页面。

首屏为“我的项目”首页：左侧项目/设置/关于，主区新建/打开、最近工程卡片和搜索排序。无项目时展示明确空状态，记录来自真实打开和保存，不扫描磁盘或伪造缩略图。关于页提供用户指定 B 站主页入口。

0.5.0 已完成工作台重构：专业布局可拖动三个分隔条，调整素材栏、属性栏宽度和时间线高度；可以收起面板，预览区提供重置布局入口。素材库增加类型筛选和排序；底部工具按选择、刀片、编辑与吸附分组，右侧“适合全片”与缩放控件共同调整时间线视野。预览保留大画面，播放区增加前后一帧操作；导出设置集中为清晰的输出参数区。交互参考 Concat（原 WolfCut），以现有 React 组件实现，没有复制其 Slint、字体、图标或效果预览资产。

普通关键帧模式仍默认一键记录当前画面、常用滑块和整组导航；专业模式增加贝塞尔图形预览、四个控制点数值与三个缓动预设。工作台布局和关键帧模式是两个独立开关，偏好跨次启动保存。手机风格保留底部分类和可展开检查器，编辑同一份工程；中英文控件均保留可访问名称。示例全部原创文字与色块，明确标为示例。退出或切换工程前保护未保存修改。

2026-09-19 的真实桌面工作台回归 7/7 通过，覆盖三处分隔条、折叠和重置、真实视频/FLAC/图片筛选、时间线适配与刀片撤销、逐帧画面、贝塞尔保存、手机布局与英文导出。1100×620 短窗口中素材区实测高度约 167.67 px，首行素材和导入按钮可见。证据见 [工作台报告](docs/verification/050-workbench.json)与 [0.5.0 验证记录](docs/VERIFICATION-050.md)。这组结果属于本机源码验证，不代表安装包已经发布或所有平台已验收。

实际截图：[中文桌面](docs/screenshots/editor-050.png)、[手机风格](docs/screenshots/mobile-050.png)、[英文桌面](docs/screenshots/editor-en-050.png)。仍保留中性深灰与薄荷强调，不以移植算法作为更换 FreeCut 品牌的理由。
