# CLI 版本记录

2026-09-07，在新变体目录执行 `npx --yes hyperframes@latest upgrade --project . --check`，退出码 0，输出：`Project pins already on hyperframes@0.8.30`。本轮仍使用 **HyperFrames 0.8.30**，未升级版本或修改原项目。

检查、截图和渲染都使用 0.8.30 的固定版本。原始探测记录在 `renders/cli-version-probe.log`。本次是已有工程的指定编辑，按 HyperFrames entry/core/cli 技能执行，用户已授权完成渲染。
