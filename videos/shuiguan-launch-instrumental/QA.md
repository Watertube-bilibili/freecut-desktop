# 无声无逐句字幕版 · 验收记录

日期：2026-09-07。本次交付为后续水管剪辑工程使用的纯视觉素材，不替换原宣传片发布资产。

- 文件：`renders/shuiguan-launch-silent-1080p.mp4`
- 1920×1080，H.264，30 fps，55.000 秒，1650 帧。
- 只有一个视频流；**音轨 0、字幕流 0**。没有配音、音乐或音效。
- 5,211,026 bytes；SHA-256：`2414600e8ff575971d6c546efe1ff14c1b892a768206aaa7215e61fadaf28653`。
- HyperFrames **0.8.30**，high 画质，4 workers，渲染耗时 1 分 46.5 秒。只对渲染子进程设置本变体 D 盘临时目录。

## 实际验证

1. 最新 CLI 只读探测确认 0.8.30 已为当前版本，没有版本升级。版本记录见 `CLI-VERSION.md`。
2. 七个场景保持原时轴：0–7、7–14、14–22、22–30、30–40、40–47、47–55 秒。HTML 中全部八条 audio 和 el-captions 挂载已移除；没有复制旁白、音乐、SRT、逐句字幕合成或缓存。
3. 完整 check 在 21 个时间点通过：lint 0 错误/0 警告，runtime 0 错误，contrast 32/32。保留原设计的一个标点遮罩边界警告和一个标题入场信息，经画面检查没有可见缺字，不改动既有场景排版。报告为 `renders/hyperframes-check.json`。
4. 对七镜中点和片尾共八张预览进行检查，逐句字幕层已消失，主文案及 @我叫水管同学 水印保留。
5. 对真正 MP4 使用 FFmpeg `-xerror` 全部解码，1650 帧成功；`blackdetect=d=0.03:pix_th=0.05` 未检出黑屏段。FFprobe 明确只有视频流，因此不存在需要依赖音量开关的静音音轨。
6. 从真正 MP4 取出 3.5、10.5、18、26、35、43.5、51、54.96 秒八张代表帧及联系表。逐场看过，底部逐句字幕不存在、水印昵称正确、画面没有黑屏。片尾全尺寸确认真实 GitHub、B站 UID 与自愿赞助文案完整。

脚本 `node scripts/verify-silent.cjs` 可重复媒体参数、结构、完整解码和抽帧检查，需要 PATH 中有 FFmpeg / FFprobe。机器验证汇总在 `renders/verification-summary.json`，实际抽帧联系表在 `renders/decoded-contact-sheet.png`，各帧也保留在 renders 中。

原发布作品、根 renders、应用代码及水管剪辑后续编辑工程均未修改。源与冻结资产可继续编辑，MP4 作为下一步真正应用内编辑与导出的输入。
