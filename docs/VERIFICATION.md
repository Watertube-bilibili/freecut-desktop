# FreeCut 验证记录

本记录区分源代码测试、实际桌面交互和原生模型推理。一次成功不代表所有素材、硬件或长项目均已验收。

## 0.3.1 中英文、右键菜单与覆盖安装

2026-09-07，Windows x64 / Electron 44.2.0。以下是本地源代码应用的验证；三平台发布构建以 [0.3.1 Release](https://github.com/Watertube-bilibili/freecut-desktop/releases/tag/v0.3.1-preview.1) 的关联 Actions 为准。

- `npm test`：118 项前端核心/国际化测试、73 项 Electron 宿主测试、22 项安装器后端测试通过，共 213 项。Windows 跳过 1 项仅适用于 Mac 的更新测试。新增默认中文、英文持久化和占位符、保留用户文字、旧安装识别、迁移回滚、缺失目录创建与缺失载荷的边界测试。
- 生产 TypeScript/Vite 构建通过。原编辑器 5 组、直接预览变换 10 组、实时视频预览 7 组和首页/退出保护 11 组均通过。真实拖动和布局切换采样未观察到黑帧，保存及取消保护仍生效。
- `regression-context-menu.cjs`：9 组真实桌面交互通过。右键对象与先前选中对象隔离且不移动播放头，分割保留关键帧并可撤销，复制/剪切/粘贴到指定时间和兼容轨道，锁轨与在用素材保护，预览右键命中，输入框快捷键、帮助窗口和菜单键盘隔离，手机与短窗口菜单贴边。工程保存重开后由实际 UI 导出 2 秒 1280×720 / 24 fps H.264/AAC，完整解码、画面像素及有声音频通过；渲染页面错误为 0。
- `regression-language.cjs`：10 组通过，包括全新配置固定中文、切换英文和跨 Electron 重启保留、非法语言 IPC 拒绝、英文原生保存/退出对话框、取消后保留修改、放弃后退出、英文工程保存重开和两种布局的窗口边界。英文原创测试工程的可见编辑器界面检查不含中文漏译，项目内容原样保存；四张 1920×1080 真实截图另有来源/散列记录。CI 默认只写独立临时目录，`--capture-promo` 才更新宣传截图。
- 首轮 Mac CI 揭示语言回归末尾错误地等待“关最后一个窗口后应用退出”。这与 macOS 保留进程的正常行为不同；测试现先验证窗口关闭取消，再调用真实 `app.quit()` 验证放弃后退出，未改应用退出逻辑或绕过保存守卫。
- `installer/smoke.cjs`：6 组真实自定义安装器验证通过，包括默认中文、英文切换与跨进程持久化、英文原生目录对话框、C/D 入口和根目录规范化。在 D 盘独立临时目录中，从不存在的多层父子目录开始真实安装并卸载，保留个人文件。测试不改写用户实际安装。
- 在本机已安装的旧 0.2.0 FreeCut 目录上只读核验了 PE/ASAR 身份，没有执行旧代码或改写旧目录。迁移测试使用独立夹具，对新包相交路径做备份和替换，取消及失败恢复旧文件，工程、模型、未知文件与旧卸载器不被认领。用户报告的 D 盘原始 ENOENT 没有重现，因此记录为创建路径验证及错误分类改进，不宣称找到其唯一原因。

视觉检查使用真实英文工程、1920×1080 与 1024×768 两种窗口，检查专业和手机风格布局、菜单边界、语言文案及英文关于页。静态设计检测因本地可选 HTML 解析模块缺失而退化，不将空结果作为完整对比度或可访问性审计。

本版未新增第三方运行依赖、竞品素材、模型或字体。英文宣传片沿用本项目原创品牌、真实应用截图与既有已记录许可的字体/动画依赖；作者署名保留 `@我叫水管同学`，英文不包含捐助诉求。工程核查仍不等于商标核准、专利自由实施法律意见或零诉讼风险承诺，既有边界见 [发布核查](RELEASE-REVIEW-030.md)。

## 0.3.0 直接预览操作、声音与分发更新

2026-09-07，Windows x64 / Electron 44.2.0，本次本机验收记录如下；三平台最终构建及准确提交以 [0.3.0 Release](https://github.com/Watertube-bilibili/freecut-desktop/releases/tag/v0.3.0-preview.1) 关联的 Actions 为准。

- `npm test`：111 项前端核心测试、73 项 Electron 宿主测试、14 项安装器后端测试通过，共 198 项；Windows 跳过 1 项仅适用于 macOS 的测试。包括预览变换、蒙版路径、音频矩阵、分段模型下载、更新 SHA-256 与重试、安装清单及回滚。Windows 实际 PowerShell 便携更新 helper 在宿主测试中替换临时可执行文件，覆盖正常启动、启动失败回滚、校验失败和中文路径；真实 Node / Electron 父进程退出后，后台更新仍完成替换和启动。
- `regression-transform.cjs`：10 组真实 Electron 交互通过。点选、拖动、缩放、旋转、锁定/隐藏保护、一次手势一次撤销、Esc 取消、关键帧编辑、手机布局与工程持久化。
- 关键帧和预览变换回归使用真实保存完成状态：通过显式暂停、释放最近工程索引提交，复现“工程已落盘但 IPC 尚未完成”的时间差，确认此时键盘/坐标点击被保存保护拦截；等待保存遮罩消失后再执行原有严格操作断言。没有通过固定延时或放宽坐标断言掩盖问题。
- `regression-effects.cjs`：7 组通过。7 种正反蒙版、羽化、叠加轨道像素、视频变换关键帧的预览/严格导出一致性、调色重置、保存重开，以及短窗口/手机布局最下方控件的可访问性。
- `regression-audio.cjs`：16 组 Web Audio 实际 PCM 与 FFmpeg 混音输出对照通过，覆盖立体声、仅左、仅右、单声道、交换、平衡及增益。16 种原创音效均经真实 FFmpeg 导入验证。
- `regression-sounds.cjs`：真实音效库显示全部 16 项，试听、重复加入同一音效并保存工程通过；2 个片段共用 1 份素材记录，避免重复素材 ID 破坏工程。
- `regression-update.cjs`：6 组真实应用更新流程通过。测试仅控制官方 GitHub URL 的网络响应、原生对话框回答与最后的安装器启动替身；使用真实下载分段、散列、主进程、preload、界面和工程原子保存。设置持久化、忙碌延后、倒计时、取消后继续编辑、保存取消/失败保护、保存成功后仅启动一次均通过。该测试不等于在用户机器上执行真实升级。
- `installer/smoke.cjs`：4 组真实自定义安装 UI 通过，包含 C/D 一键入口请求、自定义根目录补全、真实临时载荷安装、取消/重试和 PowerShell 卸载。C/D 请求在此测试中截获，避免改写实际用户目录；临时安装使用独立目录。后端新增单运行环境的原位载荷准备、原始 app.asar 字节保持和损坏载荷拒绝测试。
- `installer/smoke-setup.cjs`：实际 Setup 文件的 4 组测试通过。独立中文路径安装 36 个文件并核对 SHA-256，启动安装后的编辑器，再由应用触发卸载；应用退出后，后台卸载删除清单内文件并保留用户文件。正式 CI 对发布产物重复此检查。macOS CI 另验证原生更新替换/回滚与实际更新 ZIP 的目录、签名、版本、架构。
- Windows 卸载快捷方式回归：旧逻辑的 8.3 路径别名及英文 Windows 下 WScript 读取中文乱码均已复现。现通过 `IShellLinkW` 读取 Unicode 属性，先按文件句柄规范路径确认归属，检查属性和执行标志并记录散列，删除前再次复验。独立测试进程实际使用 ACP1252，直接编译生产读取器核对 8 条真实快捷方式的中文信息；3 条归属当前安装的默认快捷方式清理成功，不同目标、自定义参数、目录链接、管理员运行和兼容执行的 5 条快捷方式保持散列不变。
- 安装页面使用同一规范化文件 URL 加载和验证 IPC，保留主进程窗口、主 frame 和 URL 身份检查。真实 Setup 回归使用独立子进程的 8.3 TEMP 别名，覆盖此前由短路径导致的 `Invalid installer IPC sender`；不更改机器环境变量。失败时记录测试状态、实际参数、解压资源散列和截图。
- 编辑器入口同样统一加载与验证的 URL，修复程序放在带 `~` 的路径时拒绝桌面 IPC 的问题。`regression-entry-url.cjs` 使用真实含波浪号、空格与中文的独立目录，检查页面地址、应用信息、工程保存、最近列表和关闭握手，并确认另一个真实窗口即使加载相同地址也仍被拒绝。Windows 另对实际打包程序执行此检查。
- 既有首页回归 11 组、编辑器回归 5 组、真实视频预览回归 7 组仍通过；快速定位/布局变化采样未观察到黑帧。TypeScript 和生产构建通过。

SenseVoice 在原公开 0.2.0 应用、中文路径和补丁打包应用中完成实际运行环境/模型安装、散列检查及中文识别。用户报告的“下载完成后立即报错”未在本机复现；因此本版记录为下载完整性、恢复和诊断加固，不宣称已复现并确定其唯一原因。下载测试只用固定上游文件与独立临时 profile，不修改已有用户模型。

公开分发使用三平台源码构建 FFmpeg；本地快速打包的供应商引擎不作为正式 Release 引擎。依赖许可核对见 [DEPENDENCY-LICENSE-AUDIT.md](DEPENDENCY-LICENSE-AUDIT.md)，品牌与模型等边界见 [RELEASE-REVIEW-030.md](RELEASE-REVIEW-030.md)。未验证事项包括所有硬件/媒体组合、长片压力、Mac AI 模型真实推理以及正式代码签名/公证。

宣传片的真实媒体检查与来源记录独立保存：首版见 `videos/shuiguan-launch/QA.md` 和 `SOURCES.md`；新版无声片的实际水管剪辑导出与完整静音检查见 `videos/shuiguan-launch-silent-edit/QA.md`，视觉输入的来源与许可见 `videos/shuiguan-launch-instrumental/`。不把动画宣传画面当成软件功能测试。

## AI 剪辑 Skill

`skills/shuiguan-cut` 通过 `quick_validate.py`。Windows 本地源码模式与实际打包应用模式均完成 5 秒素材导入、工程保存和真实应用 UI 导出；横屏 1280×720、竖屏 720×1280 / 24 fps 成片严格完整解码，自动化实例的更新服务关闭、配置目录隔离。独立智能体使用另一组原创视频及双声道音频完成 6 秒、720×720 / 25 fps 配方，逐像素核对入点、半速播放、位置关键帧与中文文字，并对音频核对左右交换、独立增益与音量关键帧；全新应用配置下重新打开和保存工程正确。

输出目录已存在时拒绝执行，原 7 个文件的散列保持不变；未知字段明确拒绝并写失败报告，不生成 MP4。脚本本身不替代产品渲染器。Mac 与 Windows CI 对同一 Skill 再执行真实应用自检，结果以 Release 关联构建为准；尚未宣称桥接所有软件功能。

## 0.2.0 首页与编辑体验更新

2026-09-06，Windows x64 / Electron 44.2.0，当前更新已完成：

- `npm test`：59 项前端核心测试、39 项宿主测试，共 98 项通过，无跳过。新增普通关键帧整组行为、关闭守卫、保存取消/失败、退出时资源保留、最近工程持久化和定位授权测试。
- `npm run build`：TypeScript 和生产构建通过。
- `scripts/regression-preview.cjs`：7 组真实 Electron + H.264 视频像素回归通过。48 次快速定位、8 次实际布局切换、两种布局各 20 次时间尺拖动，采样黑帧为 0；最后画面与最新请求一致。另验证媒体失败保留已显示画面、画布重新挂载复用解码、预览与严格导出并行互不干扰。
- `scripts/regression-home.cjs`：11 组真实桌面回归通过。覆盖首页/设置/关于、B站/GitHub 固定链接、设置重启持久化、0 片段改名的未保存状态、返回首页取消与保存、最近工程重开/移除/定位，以及原生关闭窗口和应用退出的保存取消、保存失败与成功写入后退出。模拟延迟文件操作，确认打开/保存不会覆盖期间产生的修改，旧项目的迟到导入不会写进新项目。
- `scripts/regression-keyframes.cjs`：5 组全应用回归通过。默认普通模式一键记录文字画面，在 2 秒处调到 175%，保存后核对 5 个属性的两组关键帧；前后跳转、模式互通不改数据、整组删除及再次保存均正确。
- 原 `scripts/smoke-desktop.cjs` 和 `scripts/regression-editor.cjs` 已适配真实首页入口与模式开关。真实素材导入、中文文字、关键帧、保存重开及 720p / 24 fps 有声 MP4 导出通过；原 5 组编辑回归仍通过。

桌面脚本使用独立临时 profile 和生成的测试工程。原生对话框、外开浏览器在测试进程中被替换为预设响应；工程 IPC、主进程退出保护、实际磁盘读写、媒体解码与导出均使用产品实现。不会操作用户的实际工程或配置。

本次三平台 CI 运行相同核心/宿主和桌面回归，再验证实际打包应用。所选构建、对应提交和发布文件校验记录以 [0.2.0 Release](https://github.com/Watertube-bilibili/freecut-desktop/releases/tag/v0.2.0-preview.1) 为准。以下保留 0.1.0 的模型与初版分发验证历史，本次没有重复下载或扩大 AI 模型验收范围。

## 0.1.0 Windows 开发环境

2026-09-06，Windows x64、Electron 44.2.0。当前源代码通过：

- `npm test`：51 项编辑核心测试、24 项桌面宿主测试，全部通过，未跳过。包括关键帧缓动和裁剪、分割淡变连续性、SRT、工程验证、下载与归档安全、取消和真实 H.264/AAC 输出。
- 真实 FFmpeg 解析 1000 个音量关键帧，并按音频样本检查全部五种缓动、保持跳点和区间边界。FFmpeg 6.1.1 与 9.0.1 均通过。
- `npm run build`：TypeScript 和生产构建通过。
- `scripts/smoke-desktop.cjs`：真实桌面导入生成的视频素材，添加缩放关键帧与中文文字，保存及重开工程，输出并解码 720p/24fps MP4；渲染页面错误为空。既可针对开发应用，也可用 `FREECUT_TEST_EXE` 指向实际打包应用。
- `scripts/regression-editor.cjs`：键盘撤销及重做、手机布局检查器的关闭按钮和 Esc、锁轨编辑保护、带关键帧及淡变的时长缩短与保存重开、错误类型/过短素材拒绝及兼容素材重连，5 组均通过。Mac 临时目录的 `/var` 和 `/private/var` 别名以实际规范路径比较。
- npm 依赖审计：当日报告 0 个已知漏洞；这不代替完整安全审计。

桌面测试使用独立测试配置和本地产生的素材。视频烟雾测试产物位于忽略的 `artifacts/smoke/`；编辑回归使用新的系统临时目录。

## 可选语音模型

原生 sherpa-onnx 在真实 Electron 子进程中测试，已解决 Electron 外部 ArrayBuffer 兼容问题。SenseVoice 完成中文和英文录音识别；Whisper tiny 完成英文识别，中文表现较弱；AISHELL-3 VITS 完成中文 WAV 合成和取消。可复现实测脚本为 `scripts/smoke-ai.cjs`，默认禁止网络下载。

Windows 桌面面板另行完成 AISHELL 生成/试听/加入音轨、SenseVoice 识别/编辑/加入字幕轨道、保存工程；音频引用和字幕时间均正确，控制台无错误。原识别存在“测试→设施”的误字，已校对后加入字幕。

ChatTTS 完成独立 Python 和 CPU 依赖准备、全部模型 SHA-256 校验、真实 WAV 自检、再次合成与媒体导入。已修复 Windows 隔离 Python 对中文输入的编码问题；测试文本“你好，这是自由剪辑的真实语音测试。”由 SenseVoice 转写并核对一致。可复现脚本为 `scripts/smoke-chattts.cjs`，模型和结果报告保存在显式指定的外部数据目录。实际桌面面板也完成生成、2.91 秒音频试听、加入时间线与工程保存，生成文件散列与内容核验样本一致。

语音质量与每段内容仍需要试听、校对。ChatTTS 模型许可限非商业用途；AISHELL-3 权重许可状态见模型文档。Mac 的可选语音模型推理尚未完成设备验收。

## 平台分发

首轮 [Actions 构建 34034700514](https://github.com/Watertube-bilibili/freecut-desktop/actions/runs/34034700514) 已证明 Windows x64、Mac arm64、Mac x64 均可从固定 FFmpeg/x264/zlib 源码构建引擎，通过当时测试并生成安装包及源码归档。

最终 [构建 34037318468](https://github.com/Watertube-bilibili/freecut-desktop/actions/runs/34037318468) 对应 `9a9ac1066134b0ffaebef4dcdc742f048f525f58`，三个平台全部成功，包括 75 项核心/宿主测试、5 组编辑回归及实际打包程序的导入、关键帧、工程保存重开和 MP4 导出。发布工作流核验三平台源码一致性、引擎源码散列及所有上传文件后，已公开 [v0.1.0-preview.1](https://github.com/Watertube-bilibili/freecut-desktop/releases/tag/v0.1.0-preview.1)，共 14 份文件。

最终 Windows 便携 EXE 另行下载到本机，实际启动确认随包引擎存在、便携模式启用、旁边的 `FreeCutData` 创建成功，关闭再打开后布局与引导状态均保留。再从同一便携文件提取应用执行完整桌面烟雾测试，真实导入、关键帧、中文文字、保存重开与 MP4 导出均成功。Windows 两种 EXE 与 Mac 两种 DMG 已下载并匹配公开 SHA-256 清单。

Windows 包未配置代码签名；Mac 包使用临时签名，未配置 Developer ID 和 Apple 公证。尚未完成完整安装/卸载矩阵、4K 长片压力、所有输入编码和所有机器的 GPU/CPU 组合测试。
